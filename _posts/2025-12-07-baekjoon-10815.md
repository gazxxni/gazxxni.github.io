---
layout: post
title: "[백준] 10815번 숫자 카드 (Python)"
date: 2025-12-07
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, 10815, 자료 구조, 정렬, 이분 탐색, 집합과 맵, 해시를 사용한 집합과 맵]
---

## 문제 링크
[https://www.acmicpc.net/problem/10815](https://www.acmicpc.net/problem/10815)

---

## 문제
숫자 카드는 정수 하나가 적혀져 있는 카드이다. 상근이는 숫자 카드 N개를 가지고 있다. 정수 M개가 주어졌을 때, 이 수가 적혀있는 숫자 카드를 상근이가 가지고 있는지 아닌지를 구하는 프로그램을 작성하시오.

---

## 입력
첫째 줄에 상근이가 가지고 있는 숫자 카드의 개수 N(1 ≤ N ≤ 500,000)이 주어진다. 둘째 줄에는 숫자 카드에 적혀있는 정수가 주어진다. 숫자 카드에 적혀있는 수는 -10,000,000보다 크거나 같고, 10,000,000보다 작거나 같다. 두 숫자 카드에 같은 수가 적혀있는 경우는 없다.

셋째 줄에는 M(1 ≤ M ≤ 500,000)이 주어진다. 넷째 줄에는 상근이가 가지고 있는 숫자 카드인지 아닌지를 구해야 할 M개의 정수가 주어지며, 이 수는 공백으로 구분되어져 있다. 이 수도 -10,000,000보다 크거나 같고, 10,000,000보다 작거나 같다

---

## 출력
첫째 줄에 입력으로 주어진 M개의 수에 대해서, 각 수가 적힌 숫자 카드를 상근이가 가지고 있으면 1을, 아니면 0을 공백으로 구분해 출력한다.

---

## 성능 요약
메모리: 250016 KB, 시간: 460 ms

---

## 풀이

### 풀이 핵심 로직
이 문제의 핵심 아이디어는 상근이가 가지고 있는 숫자 카드를 해시맵(딕셔너리)로 저장하여, 주어진 M개의 수가 이 해시맵에 존재하는지를 빠르게 확인하는 것입니다. 해시맵을 이용하면 평균 O(1) 시간에 검색할 수 있어, 효율적으로 문제를 해결할 수 있습니다.

### 동작 과정
1. 숫자 카드의 개수 N을 입력받고, 그 다음에 N개의 숫자 카드를 입력받습니다.
   - 예를 들어, 입력이 다음과 같다고 가정합니다:
     ```
     5
     6 3 2 10 -10
     ```
   - 이 경우, 상근이는 숫자 카드 6, 3, 2, 10, -10을 가지고 있습니다.

2. M의 값을 입력받고, 상근이가 확인해야 할 M개의 수를 입력받습니다.
   - 예를 들어, 입력이 다음과 같다고 가정합니다:
     ```
     8
     10 9 -5 2 3 4 5 -10
     ```
   - 이 경우, 확인해야 할 숫자는 10, 9, -5, 2, 3, 4, 5, -10입니다.

3. 상근이가 가지고 있는 숫자 카드를 해시맵에 저장합니다. 여기서는 각 숫자를 키로 하고, 값은 사용하지 않으므로 0으로 설정합니다.
   - 결과적으로 해시맵은 {6: 0, 3: 0, 2: 0, 10: 0, -10: 0}이 됩니다.

4. 각 M개의 수를 해시맵에서 검색합니다. 만약 해당 숫자가 해시맵에 존재하면 1을, 존재하지 않으면 0을 결과 리스트에 추가합니다.
   - 예를 들어, 10은 존재하므로 1, 9는 존재하지 않으므로 0, -5는 존재하지 않으므로 0, 2는 존재하므로 1, 3은 존재하므로 1, 4는 존재하지 않으므로 0, 5는 존재하지 않으므로 0, -10은 존재하므로 1을 추가합니다.
   - 결과는 [1, 0, 0, 1, 1, 0, 0, 1]이 됩니다.

5. 최종 결과를 공백으로 구분하여 출력합니다.
   - 출력: `1 0 0 1 1 0 0 1`

### 시간 복잡도
이 알고리즘의 시간 복잡도는 O(N + M)입니다. 여기서 N은 상근이가 가진 숫자 카드의 개수, M은 확인해야 할 숫자의 개수입니다. 해시맵에 숫자를 저장하고 검색하는 과정이 평균적으로 O(1) 시간이 걸리기 때문에, 전체적으로 이 복잡도를 갖습니다.

---

## 코드
```python
n = int(input())
arr = list(map(int, input().split()))
m = int(input())
brr = list(map(int, input().split()))

dic = {}
for i in range(len(arr)):
    dic[arr[i]] = 0

ans = []
for i in range(m):
    if brr[i] not in dic:
        ans.append(0)
    else:
        ans.append(1)
        
print(*ans)
```