---
layout: post
title: "[백준] 18258번 큐 2 (Python)"
date: 2025-12-08
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "18258", 자료 구조, 큐]
---

## 문제 링크
[https://www.acmicpc.net/problem/18258](https://www.acmicpc.net/problem/18258)

---

## 문제
정수를 저장하는 큐를 구현한 다음, 입력으로 주어지는 명령을 처리하는 프로그램을 작성하시오.

명령은 총 여섯 가지이다.


	push X: 정수 X를 큐에 넣는 연산이다.
	pop: 큐에서 가장 앞에 있는 정수를 빼고, 그 수를 출력한다. 만약 큐에 들어있는 정수가 없는 경우에는 -1을 출력한다.
	size: 큐에 들어있는 정수의 개수를 출력한다.
	empty: 큐가 비어있으면 1, 아니면 0을 출력한다.
	front: 큐의 가장 앞에 있는 정수를 출력한다. 만약 큐에 들어있는 정수가 없는 경우에는 -1을 출력한다.
	back: 큐의 가장 뒤에 있는 정수를 출력한다. 만약 큐에 들어있는 정수가 없는 경우에는 -1을 출력한다.

---

## 입력
첫째 줄에 주어지는 명령의 수 N (1 ≤ N ≤ 2,000,000)이 주어진다. 둘째 줄부터 N개의 줄에는 명령이 하나씩 주어진다. 주어지는 정수는 1보다 크거나 같고, 100,000보다 작거나 같다. 문제에 나와있지 않은 명령이 주어지는 경우는 없다.

---

## 출력
출력해야하는 명령이 주어질 때마다, 한 줄에 하나씩 출력한다.

---

## 성능 요약
메모리: 162284 KB, 시간: 720 ms

---

## 풀이

### 풀이 핵심 로직
이 문제는 큐의 기본적인 연산을 구현하는 것입니다. 파이썬의 `collections.deque`를 사용하여 큐의 삽입 및 삭제 작업을 효율적으로 처리하며, 각 명령에 따라 큐의 상태를 업데이트하고 필요한 결과를 출력합니다.

### 동작 과정
1. 입력으로 주어지는 명령의 수 `N`을 읽습니다.
2. 그 후 `N`개의 명령을 처리합니다.
   - `push X`: 큐의 끝에 정수 `X`를 추가합니다.
   - `pop`: 큐의 앞에서 정수를 제거하고 그 값을 출력합니다. 큐가 비어있으면 `-1`을 출력합니다.
   - `size`: 큐에 있는 정수의 개수를 출력합니다.
   - `empty`: 큐가 비어있으면 `1`을 출력하고, 아니면 `0`을 출력합니다.
   - `front`: 큐의 가장 앞에 있는 정수를 출력합니다. 큐가 비어있으면 `-1`을 출력합니다.
   - `back`: 큐의 가장 뒤에 있는 정수를 출력합니다. 큐가 비어있으면 `-1`을 출력합니다.
3. 이 모든 명령은 O(1)의 시간 복잡도로 처리할 수 있습니다.

예시 입력:
```
7
push 1
push 2
front
back
size
pop
empty
```

단계별 설명:
- `push 1`: 큐에 `1` 추가 → 큐 상태: `[1]`
- `push 2`: 큐에 `2` 추가 → 큐 상태: `[1, 2]`
- `front`: 큐의 앞 요소 `1` 출력 → 출력: `1`
- `back`: 큐의 뒤 요소 `2` 출력 → 출력: `2`
- `size`: 큐의 크기 `2` 출력 → 출력: `2`
- `pop`: 큐의 앞 요소 `1` 제거하고 출력 → 출력: `1`, 큐 상태: `[2]`
- `empty`: 큐가 비어있지 않으므로 `0` 출력 → 출력: `0`

### 시간 복잡도
O(1) (각 명령어는 큐의 기본 연산으로 상수 시간에 처리됨)

---

## 코드
```python
import sys
from collections import deque
input = sys.stdin.readline
n = int(input())
q = deque()

for _ in range(n):
    command = input().split()
    
    a = command[0]
    
    if a == 'push':
        b = int(command[1])
        q.append(b)
        
    elif a == 'pop':
        if q:
            print(q.popleft())
        else:
            print(-1)
            
    elif a == 'size':
        print(len(q))
        
    elif a == 'empty':
        if not q:
            print(1)
        else:
            print(0)
            
    elif a == 'front':
        if q:
            print(q[0])
        else:
            print(-1)
            
    elif a == 'back':
        if q:
            print(q[-1])
        else:
            print(-1)
```