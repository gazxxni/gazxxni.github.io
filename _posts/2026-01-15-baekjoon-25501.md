---
layout: post
title: "[백준] 25501번 재귀의 귀재 (Python)"
date: 2026-01-15
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "25501", 구현, 문자열, 재귀]
---

## 문제 링크
[https://www.acmicpc.net/problem/25501](https://www.acmicpc.net/problem/25501)

---

## 문제
정휘는 후배들이 재귀 함수를 잘 다루는 재귀의 귀재인지 알아보기 위해 재귀 함수와 관련된 문제를 출제하기로 했다.

팰린드롬이란, 앞에서부터 읽었을 때와 뒤에서부터 읽었을 때가 같은 문자열을 말한다. 팰린드롬의 예시로 AAA, ABBA, ABABA 등이 있고, 팰린드롬이 아닌 문자열의 예시로 ABCA, PALINDROME 등이 있다.

어떤 문자열이 팰린드롬인지 판별하는 문제는 재귀 함수를 이용해 쉽게 해결할 수 있다. 아래 코드의 isPalindrome 함수는 주어진 문자열이 팰린드롬이면 1, 팰린드롬이 아니면 0을 반환하는 함수다.

#include

---

## 입력
첫째 줄에 테스트케이스의 개수 T$T$가 주어진다. (1≤T≤1000$1 \leq T \leq 1\,000$)

둘째 줄부터 T$T$개의 줄에 알파벳 대문자로 구성된 문자열 S$S$가 주어진다. (1≤|S|≤1000$1 \leq \vert S\vert \leq 1\,000$)

---

## 출력
각 테스트케이스마다, isPalindrome 함수의 반환값과 recursion 함수의 호출 횟수를 한 줄에 공백으로 구분하여 출력한다.

---

## 성능 요약
메모리: 111016 KB, 시간: 180 ms

---

## 풀이

### 풀이 핵심 로직
이 문제의 핵심은 주어진 문자열이 팰린드롬인지 확인하기 위해 재귀 함수를 사용하여 문자열의 양 끝을 비교하는 것이다. 재귀적으로 문자열의 첫 문자와 마지막 문자를 비교하고, 같지 않으면 0을 반환하고, 같으면 다음 문자 쌍을 비교하기 위해 재귀 호출을 계속한다. 또한 호출 횟수를 카운트하여 결과와 함께 반환한다.

### 동작 과정
예를 들어, 입력 문자열이 "ABBA"인 경우를 살펴보자.

1. `isPalindrome("ABBA")`가 호출된다. 
   - `recursion("ABBA", 0, 3, 1)` 호출 (l=0, r=3, cnt=1)
2. s[0] ('A')와 s[3] ('A')를 비교, 같으므로 다음 호출로 이동.
   - `recursion("ABBA", 1, 2, 2)` 호출 (l=1, r=2, cnt=2)
3. s[1] ('B')와 s[2] ('B')를 비교, 같으므로 다음 호출로 이동.
   - `recursion("ABBA", 2, 1, 3)` 호출 (l=2, r=1, cnt=3)
4. 이제 l (2) >= r (1)이므로, 팰린드롬이므로 1과 호출 횟수 3을 반환.
5. 최종적으로 `isPalindrome("ABBA")`는 (1, 3)을 반환하고, 출력은 "1 3"이 된다.

이와 같은 방식으로 각 문자열에 대해 팰린드롬 여부와 호출 횟수를 계산한다.

### 시간 복잡도
이 알고리즘의 시간 복잡도는 O(n)이다. 문자열의 길이에 따라 최대 n/2 번의 호출이 발생하며, 각 호출에서 상수 시간에 연산이 이루어지기 때문이다.

---

## 코드
```python
def recursion(s, l, r, cnt):
    if l >= r:
        return 1, cnt
    elif s[l] != s[r]:
        return 0, cnt
    else:
        return recursion(s, l + 1, r - 1, cnt + 1)

def isPalindrome(s):
    return recursion(s, 0, len(s) - 1, 1)

t = int(input())
for _ in range(t):
    s = input().rstrip()
    print(*isPalindrome(s))

```