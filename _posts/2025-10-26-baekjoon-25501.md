---
layout: post
title: "[백준] 25501번 재귀의 귀재 (Python)"
date: 2025-10-26
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "25501", 구현, 문자열, 재귀]
---

## 문제 링크
[https://www.acmicpc.net/problem/25501](https://www.acmicpc.net/problem/25501)

---

## 문제
정휘는 후배들이 재귀 함수를 잘 다루는 재귀의 귀재인지 알아보기 위해 재귀 함수와 관련된 문제를 출제하기로 했다.

팰린드롬이란, 앞에서부터 읽었을 때와 뒤에서부터 읽었을 때가 같은 문자열을 말한다. 팰린드롬의 예시로 AAA, ABBA, ABABA 등이 있고, 팰린드롬이 아닌 문자열의 예시로 ABCA, PALINDROME 등이 있다.

어떤 문자열이 팰린드롬인지 판별하는 문제는 재귀 함수를 이용해 쉽게 해결할 수 있다. 아래 코드의 isPalindrome 함수는 주어진 문자열이 팰린드롬이면 1, 팰린드롬이 아니면 0을 반환하는 함수다.

#include

---

## 입력
첫째 줄에 테스트케이스의 개수 T$T$가 주어진다. (1≤T≤1000$1 \leq T \leq 1\,000$)

둘째 줄부터 T$T$개의 줄에 알파벳 대문자로 구성된 문자열 S$S$가 주어진다. (1≤|S|≤1000$1 \leq \vert S\vert \leq 1\,000$)

---

## 출력
각 테스트케이스마다, isPalindrome 함수의 반환값과 recursion 함수의 호출 횟수를 한 줄에 공백으로 구분하여 출력한다.

---

## 성능 요약
메모리: 111016 KB, 시간: 180 ms

---

## 풀이

### 풀이 핵심 로직
문자열이 팰린드롬인지 확인하기 위해 재귀 함수를 사용한다. 주어진 문자열의 양 끝 문자를 비교하고, 같으면 내부 문자열에 대해 다시 재귀 호출을 진행하여 중앙까지 도달할 때까지 반복한다. 문자열의 길이와 호출 횟수를 함께 반환하여 팰린드롬 여부와 재귀 호출 횟수를 기록한다.

### 동작 과정
1. 입력으로 "ABBA"라는 문자열이 주어졌다고 가정해보자.
2. `isPalindrome` 함수가 호출되면, `recursion` 함수가 `l=0` (첫 문자 'A'), `r=3` (마지막 문자 'A')로 호출된다.
3. 두 문자가 같으므로, `recursion`은 `l`을 1 증가시키고 `r`을 1 감소시켜 `l=1` (두 번째 문자 'B'), `r=2` (세 번째 문자 'B')로 다시 호출된다.
4. 다시 두 문자가 같으므로, `recursion`은 `l=2`, `r=1`로 호출하고, 이제 `l`이 `r`보다 크므로 팰린드롬임을 확인하고 1과 호출 횟수(3)를 반환한다.
5. 최종적으로 `isPalindrome`은 (1, 3)을 반환하여 "1 3"을 출력하게 된다.

입력 예시:
```
3
ABBA
ABC
A
```
출력:
```
1 3
0 2
1 1
```

### 시간 복잡도
문자열의 길이를 n이라고 할 때, 각 문자를 한 번씩만 비교하므로 시간 복잡도는 O(n)이다. 전체 테스트 케이스에 대해 T가 최대 1000개일 경우, 전체 시간 복잡도는 O(T * n)으로 최악의 경우 O(1,000,000)이다.

---

## 코드
```python
def recursion(s, l, r, cnt):
    if l >= r:
        return 1, cnt
    elif s[l] != s[r]:
        return 0, cnt
    else:
        return recursion(s, l + 1, r - 1, cnt + 1)

def isPalindrome(s):
    return recursion(s, 0, len(s) - 1, 1)

t = int(input())
for _ in range(t):
    s = input().rstrip()
    print(*isPalindrome(s))

```