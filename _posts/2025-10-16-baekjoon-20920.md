---
layout: post
title: "[백준] 20920번 영단어 암기는 괴로워 (Python)"
date: 2025-10-16
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "20920", 자료 구조, 문자열, 정렬, 집합과 맵, 해시를 사용한 집합과 맵, 트리를 사용한 집합과 맵]
---

## 문제 링크
[https://www.acmicpc.net/problem/20920](https://www.acmicpc.net/problem/20920)

---

## 문제
화은이는 이번 영어 시험에서 틀린 문제를 바탕으로 영어 단어 암기를 하려고 한다. 그 과정에서 효율적으로 영어 단어를 외우기 위해 영어 단어장을 만들려 하고 있다. 화은이가 만들고자 하는 단어장의 단어 순서는 다음과 같은 우선순위를 차례로 적용하여 만들어진다.


	자주 나오는 단어일수록 앞에 배치한다.
	해당 단어의 길이가 길수록 앞에 배치한다.
	알파벳 사전 순으로 앞에 있는 단어일수록 앞에 배치한다


 M$M$보다 짧은 길이의 단어의 경우 읽는 것만으로도 외울 수 있기 때문에 길이가 M$M$이상인 단어들만 외운다고 한다. 화은이가 괴로운 영단어 암기를 효율적으로 할 수 있도록 단어장을 만들어 주자.

---

## 입력
첫째 줄에는 영어 지문에 나오는 단어의 개수 N$N$과 외울 단어의 길이 기준이 되는 M$M$이 공백으로 구분되어 주어진다. (1≤N≤100000$1 \leq N \leq 100\,000$, 1≤M≤10$1 \leq M \leq 10$)

둘째 줄부터 N+1$N+1$번째 줄까지 외울 단어를 입력받는다. 이때의 입력은 알파벳 소문자로만 주어지며 단어의 길이는 10$10$을 넘지 않는다.

단어장에 단어가 반드시 1개 이상 존재하는 입력만 주어진다.

---

## 출력
화은이의 단어장에 들어 있는 단어를 단어장의 앞에 위치한 단어부터 한 줄에 한 단어씩 순서대로 출력한다.

---

## 성능 요약
메모리: 141212 KB, 시간: 396 ms

---

## 풀이

### 풀이 핵심 로직
이 문제는 주어진 단어 목록에서 특정 길이 이상인 단어를 필터링하고, 그러한 단어들을 빈도수, 길이, 그리고 알파벳 순으로 정렬하는 과정을 요구한다. 이를 위해 해시맵을 사용하여 단어의 빈도수를 카운트하고, 그 빈도수를 기반으로 단어들을 그룹화한 뒤, 각 그룹 내에서 정렬을 수행하여 최종적으로 원하는 순서로 출력한다.

### 동작 과정
1. **입력 처리**: 첫 줄에서 단어의 개수 \( N \)과 최소 길이 \( M \)을 입력받는다. 그 다음 \( N \)개의 단어를 입력받는다.
2. **필터링 및 카운트**: 각 단어의 길이가 \( M \) 이상인 경우에만 해시맵 `dic`에 추가하고, 해당 단어의 카운트를 증가시킨다.
   - 예: 입력으로 `apple`, `banana`, `cherry`, `date`가 주어지고 \( M = 5 \)일 때, `apple`, `banana`, `cherry`만 카운트에 포함된다.
3. **그룹화**: 빈도수에 따라 단어들을 그룹화하기 위해 또 다른 해시맵 `dic2`를 사용한다. 빈도수를 키로 하고, 해당 빈도수의 단어들을 리스트로 저장한다.
4. **정렬**: 각 빈도수 그룹에서 단어들을 길이(내림차순)와 알파벳 순(오름차순)으로 정렬한다.
5. **최종 정렬**: 빈도수 그룹을 빈도수(내림차순)로 정렬한 후, 각 그룹에서 정렬된 단어들을 출력한다.

예시 입력:
```
5 5
apple
banana
peach
grape
cherry
```
- 빈도수 카운트: `{'apple': 1, 'banana': 1, 'cherry': 1}`
- 그룹화: `dic2 = {1: ['apple', 'banana', 'cherry']}`
- 정렬 후 출력:
```
cherry
banana
apple
```

### 시간 복잡도
- 입력받기: \( O(N) \)
- 단어 카운트: \( O(N) \)
- 그룹화 및 정렬: 각 빈도수 그룹의 단어 수를 고려할 때, 최악의 경우 \( O(N \log N) \) (단어 수가 많고 모든 단어가 다를 경우)
- 최종 출력: \( O(N) \)

따라서, 전체 시간 복잡도는 \( O(N \log N) \)이다.

---

## 코드
```python
import sys
input = sys.stdin.readline
from collections import defaultdict

n, m = map(int, input().split())

dic = defaultdict(int)
for _ in range(n):
    a = input().rstrip()
    if len(a) >= m:
        dic[a] += 1
    
dic2 = defaultdict(list)
for k, v in dic.items():
    dic2[v].append(k)
    
for k, v in dic2.items():
    v.sort(key=lambda x: (-len(x), x))
    
a = sorted(dic2.items(), key=lambda x:x[0], reverse=True)

for i, j in a:
    for k in j:
        print(k)
```