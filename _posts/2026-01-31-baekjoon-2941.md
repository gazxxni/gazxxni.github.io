---
layout: post
title: "[백준] 2941번 크로아티아 알파벳 (Python)"
date: 2026-01-31
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "2941", 구현, 문자열]
---

## 문제 링크
[https://www.acmicpc.net/problem/2941](https://www.acmicpc.net/problem/2941)

---

## 문제
예전에는 운영체제에서 크로아티아 알파벳을 입력할 수가 없었다. 따라서, 다음과 같이 크로아티아 알파벳을 변경해서 입력했다.


	
		
			크로아티아 알파벳
			변경
		
	
	
		
			č
			c=
		
		
			ć
			c-
		
		
			dž
			dz=
		
		
			đ
			d-
		
		
			lj
			lj
		
		
			nj
			nj
		
		
			š
			s=
		
		
			ž
			z=
		
	


예를 들어, ljes=njak은 크로아티아 알파벳 6개(lj, e, š, nj, a, k)로 이루어져 있다. 단어가 주어졌을 때, 몇 개의 크로아티아 알파벳으로 이루어져 있는지 출력한다.

dž는 무조건 하나의 알파벳으로 쓰이고, d와 ž가 분리된 것으로 보지 않는다. lj와 nj도 마찬가지이다. 위 목록에 없는 알파벳은 한 글자씩 센다.

---

## 입력
첫째 줄에 최대 100글자의 단어가 주어진다. 알파벳 소문자와 '-', '='로만 이루어져 있다.

단어는 크로아티아 알파벳으로 이루어져 있다. 문제 설명의 표에 나와있는 알파벳은 변경된 형태로 입력된다.

---

## 출력
입력으로 주어진 단어가 몇 개의 크로아티아 알파벳으로 이루어져 있는지 출력한다.

---

## 성능 요약
메모리: 108384 KB, 시간: 76 ms

---

## 풀이

### 풀이 핵심 로직
크로아티아 알파벳을 구성하는 특정 문자열 조합을 찾아서 이를 하나의 알파벳으로 간주하는 방식으로 문제를 해결합니다. 문자열을 차례로 검사하면서 조합이 발견될 때마다 카운트를 증가시키고, 해당 조합의 길이에 맞춰 인덱스를 이동합니다.

### 동작 과정
1. 입력 문자열을 읽어옵니다. 예를 들어, 입력이 `ljes=njak`라고 가정합니다.
2. 크로아티아 알파벳 조합을 담고 있는 리스트 `arr`를 정의합니다.
3. 인덱스 `idx`를 0으로 초기화하고 카운트 `cnt`를 0으로 초기화합니다.
4. 문자열의 길이만큼 반복하며 아래의 조건을 체크합니다:
   - 현재 인덱스에서 3글자(`dz=`)가 크로아티아 알파벳 리스트에 있는지 확인합니다. 만약 있다면, 카운트를 1 증가시키고 인덱스를 3만큼 이동합니다.
   - 그렇지 않으면 현재 인덱스에서 2글자(`lj`, `nj`, `s=`, `z=`)가 리스트에 있는지 확인합니다. 있으면 카운트를 1 증가시키고 인덱스를 2만큼 이동합니다.
   - 두 조건이 모두 해당되지 않으면 현재 문자를 하나의 알파벳으로 간주하고 카운트를 1 증가시키며 인덱스를 1만큼 이동합니다.
5. 모든 문자를 검사한 후 최종 카운트를 출력합니다. 

예시 입력 `ljes=njak`의 경우:
- `l` → 카운트 1
- `j` → 카운트 2
- `e` → 카운트 3
- `s` → 카운트 4
- `=` → `s=`로 카운트 5
- `n` → 카운트 6
- `j` → 카운트 7
- `a` → 카운트 8
- `k` → 카운트 9 (최종 카운트는 6)

### 시간 복잡도
이 알고리즘의 시간 복잡도는 O(n)입니다. 여기서 n은 입력 문자열의 길이입니다. 문자열을 한 번 순회하면서 각 크로아티아 알파벳 조합을 확인하므로 효율적으로 작동합니다.

---

## 코드
```python
arr = ['c=', 'c-', 'dz=', 'd-', 'lj', 'nj', 's=', 'z=']

string = input().rstrip()

cnt = 0
idx = 0
while idx <= len(string) - 1:
    if string[idx:idx + 3] in arr:
            cnt += 1
            idx += 3
            
    elif string[idx:idx + 2] in arr:
            cnt += 1
            idx += 2
        
    else:
        cnt += 1
        idx += 1
            
print(cnt)
```