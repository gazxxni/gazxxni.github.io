---
layout: post
title: "[백준] 10986번 나머지 합 (Python)"
date: 2025-12-09
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "10986", 수학, 누적 합]
---

## 문제 링크
[https://www.acmicpc.net/problem/10986](https://www.acmicpc.net/problem/10986)

---

## 문제
수 N개 A1, A2, ..., AN이 주어진다. 이때, 연속된 부분 구간의 합이 M으로 나누어 떨어지는 구간의 개수를 구하는 프로그램을 작성하시오.

즉, Ai + ... + Aj (i ≤ j) 의 합이 M으로 나누어 떨어지는 (i, j) 쌍의 개수를 구해야 한다.

---

## 입력
첫째 줄에 N과 M이 주어진다. (1 ≤ N ≤ 106, 2 ≤ M ≤ 103)

둘째 줄에 N개의 수 A1, A2, ..., AN이 주어진다. (0 ≤ Ai ≤ 109)

---

## 출력
첫째 줄에 연속된 부분 구간의 합이 M으로 나누어 떨어지는 구간의 개수를 출력한다.

---

## 성능 요약
메모리: 258100 KB, 시간: 356 ms

---

## 풀이

### 풀이 핵심 로직
문제는 연속된 부분 구간의 합이 M으로 나누어 떨어지는 경우를 찾는 것이다. 이를 위해, 구간의 합을 누적합으로 계산하고, 그 나머지를 이용해 같은 나머지를 가진 누적합의 개수를 세면 된다. 즉, 두 누적합이 같은 나머지를 가지면 그 사이의 구간 합이 M으로 나누어 떨어진다는 성질을 이용한다.

### 동작 과정
1. N과 M의 값을 입력받는다.
2. N개의 수 A를 입력받는다.
3. `remainder_count` 배열을 만들어 M의 나머지 값을 카운트할 준비를 한다. `remainder_count[0]`은 1로 초기화하여, 누적합이 처음부터 M으로 나누어 떨어지는 경우를 고려한다.
4. 각 원소를 순회하며 누적합을 계산하고, 그 누적합을 M으로 나눈 나머지를 구한다. 각 나머지의 카운트를 업데이트한다.
5. 각 나머지별로 카운트를 확인하여, 그 카운트가 2 이상인 경우에만 조합을 통해 구간의 개수를 추가한다.
6. 결과를 출력한다.

#### 예시 입력:
```
5 3
1 2 3 1 2
```
- N = 5, M = 3
- A = [1, 2, 3, 1, 2]

##### 단계별 설명:
- 누적합: [1, 3, 6, 7, 9]
- 나머지 계산:
  - 1 % 3 = 1
  - 3 % 3 = 0
  - 6 % 3 = 0
  - 7 % 3 = 1
  - 9 % 3 = 0
- 나머지 카운트: 
  - 0 → 3회
  - 1 → 2회
- 나머지가 0인 경우 (3회)에서 (3 * (3 - 1)) // 2 = 3 쌍을 만들 수 있다.
- 나머지가 1인 경우 (2회)에서 (2 * (2 - 1)) // 2 = 1 쌍을 만들 수 있다.

결과적으로, 총 구간의 개수는 3 + 1 = 4가 된다.

### 시간 복잡도
O(N) - N개의 수를 한 번만 순회하여 누적합과 나머지를 계산하므로, 시간 복잡도는 O(N)이다.

---

## 코드
```python
import sys
input = sys.stdin.readline

'''
1. 구간 합이 M으로 나누어 떨어진다는 것은 
(prefix_sum[j] - prefix_sum[i-1]) % M == 0이라는 뜻

2. 이 식을 이항하면 prefix_sum[j] % M == prefix_sum[i-1] % M

3. 즉, 누적합을 M으로 나눈 나머지가 같은 인덱스들을 찾으면 됨
'''
n, m = map(int, input().split())
arr = list(map(int, input().split()))

# 1. 나머지를 카운트할 배열 또는 딕셔너리 준비
remainder_count = [0] * m

# 2. 누적합 변수 초기화
current_sum = 0

# 3. 나머지가 0인 경우를 미리 카운트
remainder_count[0] = 1 

# 4. 수열을 순회하면서 누적합과 나머지 계산
for x in arr:
    current_sum += x
    r = current_sum % m
    remainder_count[r] += 1

ans = 0
for cnt in remainder_count:
    # 해당 나머지를 가진 누적합의 개수가 2개 이상일 때만 쌍을 만들 수 있음
    if cnt > 1:
        ans += (cnt * (cnt - 1)) // 2

print(ans)
```