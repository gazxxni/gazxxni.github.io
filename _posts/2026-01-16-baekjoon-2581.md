---
layout: post
title: "[백준] 2581번 소수 (Python)"
date: 2026-01-16
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "2581", 소수 판정, 정수론, 수학]
---

## 문제 링크
[https://www.acmicpc.net/problem/2581](https://www.acmicpc.net/problem/2581)

---

## 문제
자연수 M과 N이 주어질 때 M이상 N이하의 자연수 중 소수인 것을 모두 골라 이들 소수의 합과 최솟값을 찾는 프로그램을 작성하시오.

예를 들어 M=60, N=100인 경우 60이상 100이하의 자연수 중 소수는 61, 67, 71, 73, 79, 83, 89, 97 총 8개가 있으므로, 이들 소수의 합은 620이고, 최솟값은 61이 된다.

---

## 입력
입력의 첫째 줄에 M이, 둘째 줄에 N이 주어진다.

M과 N은 10,000이하의 자연수이며, M은 N보다 작거나 같다.

---

## 출력
M이상 N이하의 자연수 중 소수인 것을 모두 찾아 첫째 줄에 그 합을, 둘째 줄에 그 중 최솟값을 출력한다. 

단, M이상 N이하의 자연수 중 소수가 없을 경우는 첫째 줄에 -1을 출력한다.

---

## 성능 요약
메모리: 109544 KB, 시간: 92 ms

---

## 풀이

### 풀이 핵심 로직
주어진 범위 [M, N] 내의 소수를 찾기 위해 에라토스테네스의 체 알고리즘을 사용합니다. 먼저, 2부터 N까지의 모든 수를 소수로 가정하여 배열을 초기화한 후, 각 소수의 배수를 false로 표시하여 소수를 판별합니다. 이후, M 이상 N 이하의 소수를 찾아 그 합과 최솟값을 계산합니다.

### 동작 과정
1. 입력으로 M과 N을 받습니다. 예를 들어 M=60, N=100.
2. 소수를 판별하기 위해 0부터 10000까지의 배열을 생성하고, 0과 1은 소수가 아니므로 false로 설정합니다.
3. 2부터 N의 제곱근까지 반복하며, 소수로 판별된 숫자의 배수를 false로 설정합니다.
   - 2부터 시작하여 2의 배수(4, 6, 8, ...)를 false로 설정합니다.
   - 다음으로 3의 배수(6, 9, 12, ...)를 false로 설정합니다.
   - 이 과정을 반복하여 N까지의 모든 소수를 판별합니다.
4. M부터 N까지의 소수를 찾아 합계와 최솟값을 계산합니다.
   - M=60부터 N=100까지 확인하여, 소수 61, 67, 71, 73, 79, 83, 89, 97을 찾습니다.
   - 이들의 합(620)과 최솟값(61)을 계산합니다.
5. 만약 소수가 없다면 -1을 출력하고, 있다면 합과 최솟값을 각각 출력합니다.

### 시간 복잡도
이 알고리즘의 시간 복잡도는 O(n log log n)입니다. 이는 에라토스테네스의 체 알고리즘의 특성으로, N까지의 소수를 찾는 데 소요되는 시간 복잡도입니다.

---

## 코드
```python
m = int(input())
n = int(input())

arr = [True] * 10001
arr[0], arr[1] = False, False

for i in range(2, int(n ** 0.5) + 1): 
    if arr[i]:
        for j in range(i * i, n + 1, i):
            arr[j] = False
             
cnt = 0
ans = 0
for i in range(m, n + 1):
    if arr[i]:
        cnt += i
        if ans == 0:
            ans = i

if ans == 0:
    print(-1)
else:
    print(cnt)
    print(ans)
```