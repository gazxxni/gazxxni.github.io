---
layout: post
title: "[백준] 24262번 알고리즘 수업 - 알고리즘의 수행 시간 1 (Python)"
date: 2026-01-15
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "24262", 구현, 시뮬레이션]
---

## 문제 링크
[https://www.acmicpc.net/problem/24262](https://www.acmicpc.net/problem/24262)

---

## 문제
오늘도 서준이는 알고리즘의 수행시간 수업 조교를 하고 있다. 아빠가 수업한 내용을 학생들이 잘 이해했는지 문제를 통해서 확인해보자.

입력의 크기 n이 주어지면 MenOfPassion 알고리즘 수행 시간을 예제 출력과 같은 방식으로 출력해보자.

MenOfPassion 알고리즘은 다음과 같다.

MenOfPassion(A[], n) {
    i = ⌊n / 2⌋;
    return A[i]; # 코드1
}

---

## 입력
첫째 줄에 입력의 크기 n(1 ≤ n ≤ 500,000)이 주어진다.

---

## 출력
첫째 줄에 코드1 의 수행 횟수를 출력한다.

둘째 줄에 코드1의 수행 횟수를 다항식으로 나타내었을 때, 최고차항의 차수를 출력한다. 단, 다항식으로 나타낼 수 없거나 최고차항의 차수가 3보다 크면 4를 출력한다.

---

## 성능 요약
메모리: 108384 KB, 시간: 80 ms

---

## 풀이

### 풀이 핵심 로직
문제에서 요구하는 `MenOfPassion` 알고리즘은 입력 배열 `A`의 중간 요소를 반환하는 매우 간단한 알고리즘입니다. 이 알고리즘의 수행 시간은 항상 일정하며, 입력 크기 `n`에 관계없이 코드1의 수행 횟수는 1회입니다. 또한, 이 알고리즘은 상수 시간에 실행되므로 다항식으로 나타낼 경우 최고차항의 차수는 0입니다.

### 동작 과정
1. `n`의 값으로 1이 주어질 경우:
   - `i = ⌊1 / 2⌋` 이므로 `i`는 0이 됩니다.
   - `A[i]` 즉, `A[0]`를 반환합니다. 이때 코드1은 1회 실행됩니다.
   
2. `n`의 값으로 500,000이 주어질 경우:
   - `i = ⌊500000 / 2⌋` 이므로 `i`는 250,000이 됩니다.
   - `A[i]` 즉, `A[250000]`를 반환합니다. 이 경우에도 코드1은 1회 실행됩니다.

이처럼 `n`의 값이 변화하더라도 코드1의 수행 횟수는 항상 1이 됩니다.

### 시간 복잡도
이 알고리즘의 시간 복잡도는 O(1)입니다. 즉, 입력 크기와 관계없이 항상 일정한 시간이 소요됩니다.

---

## 코드
```python
n = int(input())
print(1)
print(0)
```