---
layout: post
title: "[백준] 2563번 색종이 (Python)"
date: 2026-01-31
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "2563", 구현]
---

## 문제 링크
[https://www.acmicpc.net/problem/2563](https://www.acmicpc.net/problem/2563)

---

## 문제
가로, 세로의 크기가 각각 100인 정사각형 모양의 흰색 도화지가 있다. 이 도화지 위에 가로, 세로의 크기가 각각 10인 정사각형 모양의 검은색 색종이를 색종이의 변과 도화지의 변이 평행하도록 붙인다. 이러한 방식으로 색종이를 한 장 또는 여러 장 붙인 후 색종이가 붙은 검은 영역의 넓이를 구하는 프로그램을 작성하시오.



예를 들어 흰색 도화지 위에 세 장의 검은색 색종이를 그림과 같은 모양으로 붙였다면 검은색 영역의 넓이는 260이 된다.

---

## 입력
첫째 줄에 색종이의 수가 주어진다. 이어 둘째 줄부터 한 줄에 하나씩 색종이를 붙인 위치가 주어진다. 색종이를 붙인 위치는 두 개의 자연수로 주어지는데 첫 번째 자연수는 색종이의 왼쪽 변과 도화지의 왼쪽 변 사이의 거리이고, 두 번째 자연수는 색종이의 아래쪽 변과 도화지의 아래쪽 변 사이의 거리이다. 색종이의 수는 100 이하이며, 색종이가 도화지 밖으로 나가는 경우는 없다

---

## 출력
첫째 줄에 색종이가 붙은 검은 영역의 넓이를 출력한다.

---

## 성능 요약
메모리: 109544 KB, 시간: 92 ms

---

## 풀이

### 풀이 핵심 로직
이 문제의 핵심 아이디어는 주어진 색종이가 도화지 위에 놓인 위치를 2차원 배열로 나타내고, 색종이가 놓인 부분을 1로 표시하여 최종적으로 1의 개수를 세는 것입니다. 도화지는 최대 100x100 크기이므로 단순히 배열을 사용하여 각 색종이가 차지하는 영역을 기록하고, 마지막에 검은색 영역의 넓이를 계산합니다.

### 동작 과정
1. 입력을 통해 색종이의 수 `n`을 받습니다.
2. 101x101 크기의 2차원 배열 `arr`을 생성합니다. (0으로 초기화)
3. 각 색종이의 위치 `(x, y)`를 입력받아, 해당 위치에서 10x10의 영역을 1로 설정합니다.
   - 예를 들어, `(x, y) = (3, 7)`인 경우, `arr[3][7]`부터 `arr[12][16]`까지 모두 1로 설정합니다.
4. 모든 색종이를 붙인 후, 2차원 배열을 순회하면서 값이 1인 요소의 개수를 세어 최종적으로 검은색 영역의 넓이를 구합니다.
5. 결과를 출력합니다.

예를 들어, 입력이 다음과 같다면:
```
3
3 7
15 7
5 2
```
- 첫 번째 색종이 `(3, 7)`가 붙여지면 `arr[3][7]`에서 `arr[12][16]`까지 1로 설정.
- 두 번째 색종이 `(15, 7)`가 붙여지면 `arr[15][7]`에서 `arr[24][16]`까지 1로 설정.
- 세 번째 색종이 `(5, 2)`가 붙여지면 `arr[5][2]`에서 `arr[14][11]`까지 1로 설정.
- 최종적으로 1의 개수를 세어 검은색 영역의 넓이를 출력합니다.

### 시간 복잡도
이 알고리즘의 시간 복잡도는 O(n), 여기서 n은 색종이의 수입니다. 각 색종이는 최대 100개의 칸을 차지하므로 색종이를 붙이는 과정에서 최대 1000번의 작업이 이루어질 수 있습니다. 최종적으로 101x101 배열을 순회하는 작업은 O(1)로 상수 시간에 이루어지므로 전체 복잡도는 O(n)으로 평가할 수 있습니다.

---

## 코드
```python
import sys
input = sys.stdin.readline

n = int(input())
arr = [[0] * 101 for _ in range(101)]

for _ in range(n):
    x, y = map(int, input().split())

    for i in range(x, x+10):
        for j in range(y, y+10):
            arr[i][j] = 1

ans = 0
for i in range(101):
    for j in range(101):
        if arr[i][j] == 1:
            ans += 1

print(ans)
```