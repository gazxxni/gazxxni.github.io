---
layout: post
title: "[백준] 1934번 최소공배수 (Python)"
date: 2025-10-13
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "1934", 수학, 정수론, 유클리드 호제법]
---

## 문제 링크
[https://www.acmicpc.net/problem/1934](https://www.acmicpc.net/problem/1934)

---

## 문제
두 자연수 A와 B에 대해서, A의 배수이면서 B의 배수인 자연수를 A와 B의 공배수라고 한다. 이런 공배수 중에서 가장 작은 수를 최소공배수라고 한다. 예를 들어, 6과 15의 공배수는 30, 60, 90등이 있으며, 최소 공배수는 30이다.

두 자연수 A와 B가 주어졌을 때, A와 B의 최소공배수를 구하는 프로그램을 작성하시오.

---

## 입력
첫째 줄에 테스트 케이스의 개수 T(1 ≤ T ≤ 1,000)가 주어진다. 둘째 줄부터 T개의 줄에 걸쳐서 A와 B가 주어진다. (1 ≤ A, B ≤ 45,000)

---

## 출력
첫째 줄부터 T개의 줄에 A와 B의 최소공배수를 입력받은 순서대로 한 줄에 하나씩 출력한다.

---

## 성능 요약
메모리: 110708 KB, 시간: 140 ms

---

## 풀이

### 풀이 핵심 로직
최소공배수(LCM)는 두 수 A와 B의 곱을 두 수의 최대공약수(GCD)로 나눈 값으로 계산할 수 있습니다. 즉, LCM(A, B) = (A * B) / GCD(A, B)라는 관계를 사용하여 효율적으로 최소공배수를 구할 수 있습니다. 유클리드 호제법을 이용해 GCD를 계산하고 이를 통해 LCM을 구합니다.

### 동작 과정
1. 테스트 케이스의 개수를 입력받습니다. (예: T = 3)
2. 각 테스트 케이스에 대해 두 자연수 A와 B를 입력받습니다.
   - **입력 예시**:
     ```
     3
     6 15
     5 10
     12 15
     ```
3. 각 쌍 (A, B)에 대해 GCD를 계산합니다.
   - GCD(6, 15) = 3
   - GCD(5, 10) = 5
   - GCD(12, 15) = 3
4. LCM을 계산합니다.
   - LCM(6, 15) = (6 * 15) / 3 = 30
   - LCM(5, 10) = (5 * 10) / 5 = 10
   - LCM(12, 15) = (12 * 15) / 3 = 60
5. 각 LCM 값을 출력합니다.
   - **출력 예시**:
     ```
     30
     10
     60
     ```

### 시간 복잡도
최대공약수를 구하는 유클리드 호제법의 시간 복잡도는 O(log(min(A, B)))입니다. 따라서 전체 테스트 케이스에 대해 LCM을 구하는 시간 복잡도는 O(T * log(min(A, B)))입니다. 여기서 T는 테스트 케이스의 개수입니다.

---

## 코드
```python
t = int(input())

def gcd(a, b):
    while b > 0:
        a, b = b, a % b
    return a

def lcm(a, b):
    return a * b / gcd(a, b)

for _ in range(t):
    a, b = map(int, input().split())
    
    print(int(lcm(a, b)))
```