---
layout: post
title: "[백준] 2581번 소수 (Python)"
date: 2025-12-09
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "2581", 소수 판정, 정수론, 수학]
---

## 문제 링크
[https://www.acmicpc.net/problem/2581](https://www.acmicpc.net/problem/2581)

---

## 문제
자연수 M과 N이 주어질 때 M이상 N이하의 자연수 중 소수인 것을 모두 골라 이들 소수의 합과 최솟값을 찾는 프로그램을 작성하시오.

예를 들어 M=60, N=100인 경우 60이상 100이하의 자연수 중 소수는 61, 67, 71, 73, 79, 83, 89, 97 총 8개가 있으므로, 이들 소수의 합은 620이고, 최솟값은 61이 된다.

---

## 입력
입력의 첫째 줄에 M이, 둘째 줄에 N이 주어진다.

M과 N은 10,000이하의 자연수이며, M은 N보다 작거나 같다.

---

## 출력
M이상 N이하의 자연수 중 소수인 것을 모두 찾아 첫째 줄에 그 합을, 둘째 줄에 그 중 최솟값을 출력한다. 

단, M이상 N이하의 자연수 중 소수가 없을 경우는 첫째 줄에 -1을 출력한다.

---

## 성능 요약
메모리: 109544 KB, 시간: 92 ms

---

## 풀이

### 풀이 핵심 로직
주어진 범위 M 이상 N 이하의 모든 자연수에서 소수를 찾기 위해 에라토스테네스의 체 알고리즘을 사용한다. 이 방법으로 2부터 N까지의 소수 여부를 미리 계산한 후, M에서 N까지의 소수를 추출하여 그 합과 최솟값을 구한다.

### 동작 과정
1. 입력으로 M과 N을 받는다. 예를 들어, M=60, N=100이라 가정한다.
2. 에라토스테네스의 체를 이용해 2부터 N(100)까지의 소수를 구한다.
   - 배열 `arr`을 생성하여 인덱스가 소수인 경우 `True`, 그렇지 않은 경우 `False`로 표시한다.
   - 2부터 √N까지 반복하며, 소수인 경우 그 배수를 `False`로 표시한다.
3. M부터 N까지의 수를 확인하여 소수인 경우를 찾는다.
   - 소수의 합을 저장하는 `cnt`와 최솟값을 저장하는 `ans` 변수를 초기화한다.
   - M부터 N까지 반복하면서 소수인 경우 `cnt`에 더하고, 최솟값이 아직 정해지지 않았다면 `ans`에 해당 수를 저장한다.
4. 만약 소수가 없었다면 -1을 출력하고, 그렇지 않다면 소수의 합과 최솟값을 출력한다.

예시 입력: 
```
60
100
```
출력:
```
620
61
```
위 예시에서 60이상 100이하의 소수는 61, 67, 71, 73, 79, 83, 89, 97로 총 8개이며 이들의 합은 620, 최솟값은 61이다.

### 시간 복잡도
이 알고리즘의 시간 복잡도는 O(N log log N)이다. 이는 에라토스테네스의 체 알고리즘에서 소수를 찾는 데 필요한 시간 복잡도이다. M과 N의 차이에 관계없이, N 크기에 비례하여 소수를 찾는 효율적인 방법을 제공한다.

---

## 코드
```python
m = int(input())
n = int(input())

arr = [True] * 10001
arr[0], arr[1] = False, False

for i in range(2, int(n ** 0.5) + 1): 
    if arr[i]:
        for j in range(i * i, n + 1, i):
            arr[j] = False
             
cnt = 0
ans = 0
for i in range(m, n + 1):
    if arr[i]:
        cnt += i
        if ans == 0:
            ans = i

if ans == 0:
    print(-1)
else:
    print(cnt)
    print(ans)
```