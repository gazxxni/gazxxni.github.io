---
layout: post
title: "[백준] 10815번 숫자 카드 (Python)"
date: 2025-10-06
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "10815", 자료 구조, 정렬, 이분 탐색, 집합과 맵, 해시를 사용한 집합과 맵]
---

## 문제 링크
[https://www.acmicpc.net/problem/10815](https://www.acmicpc.net/problem/10815)

---

## 문제
숫자 카드는 정수 하나가 적혀져 있는 카드이다. 상근이는 숫자 카드 N개를 가지고 있다. 정수 M개가 주어졌을 때, 이 수가 적혀있는 숫자 카드를 상근이가 가지고 있는지 아닌지를 구하는 프로그램을 작성하시오.

---

## 입력
첫째 줄에 상근이가 가지고 있는 숫자 카드의 개수 N(1 ≤ N ≤ 500,000)이 주어진다. 둘째 줄에는 숫자 카드에 적혀있는 정수가 주어진다. 숫자 카드에 적혀있는 수는 -10,000,000보다 크거나 같고, 10,000,000보다 작거나 같다. 두 숫자 카드에 같은 수가 적혀있는 경우는 없다.

셋째 줄에는 M(1 ≤ M ≤ 500,000)이 주어진다. 넷째 줄에는 상근이가 가지고 있는 숫자 카드인지 아닌지를 구해야 할 M개의 정수가 주어지며, 이 수는 공백으로 구분되어져 있다. 이 수도 -10,000,000보다 크거나 같고, 10,000,000보다 작거나 같다

---

## 출력
첫째 줄에 입력으로 주어진 M개의 수에 대해서, 각 수가 적힌 숫자 카드를 상근이가 가지고 있으면 1을, 아니면 0을 공백으로 구분해 출력한다.

---

## 성능 요약
메모리: 250016 KB, 시간: 460 ms

---

## 풀이

### 풀이 핵심 로직
이 문제의 핵심 아이디어는 상근이가 가지고 있는 숫자 카드를 해시맵(딕셔너리)로 저장하여, M개의 수가 상근이가 가진 카드에 있는지 빠르게 조회하는 것입니다. 해시맵을 사용하면 평균적으로 O(1)의 시간 복잡도로 카드 존재 여부를 확인할 수 있습니다.

### 동작 과정
1. 먼저 상근이가 가진 카드의 개수 N을 입력받고, 카드에 적혀있는 정수들을 리스트 `arr`에 저장합니다.
2. M을 입력받고, 상근이가 확인하고자 하는 수를 리스트 `brr`에 저장합니다.
3. 카드 리스트 `arr`의 각 숫자를 해시맵 `dic`의 키로 추가하여 상근이가 가진 카드 목록을 만듭니다.
4. 리스트 `brr`의 각 숫자에 대해 해시맵 `dic`에서 해당 숫자가 존재하는지 확인합니다. 존재하면 1, 아니면 0을 결과 리스트 `ans`에 추가합니다.
5. 마지막으로 결과 리스트 `ans`를 공백으로 구분하여 출력합니다.

**예시 입력:**
```
5
6 3 2 10 -10
8
10 9 -5 2 3 4 5 -10
```
**단계별 설명:**
- N = 5, arr = [6, 3, 2, 10, -10] → 해시맵 dic = {6: 0, 3: 0, 2: 0, 10: 0, -10: 0}
- M = 8, brr = [10, 9, -5, 2, 3, 4, 5, -10]
- brr의 각 숫자에 대해 dic에서 존재 여부 확인:
  - `10` → 존재 → 1 추가
  - `9` → 존재하지 않음 → 0 추가
  - `-5` → 존재하지 않음 → 0 추가
  - `2` → 존재 → 1 추가
  - `3` → 존재 → 1 추가
  - `4` → 존재하지 않음 → 0 추가
  - `5` → 존재하지 않음 → 0 추가
  - `-10` → 존재 → 1 추가
- 최종 결과 ans = [1, 0, 0, 1, 1, 0, 0, 1] 출력

### 시간 복잡도
이 알고리즘의 시간 복잡도는 O(N + M)입니다. N개의 숫자를 해시맵에 추가하는 데 O(N) 시간이 걸리고, M개의 숫자를 조회하는 데 O(M) 시간이 걸리므로 전체적으로 O(N + M)이 됩니다.

---

## 코드
```python
n = int(input())
arr = list(map(int, input().split()))
m = int(input())
brr = list(map(int, input().split()))

dic = {}
for i in range(len(arr)):
    dic[arr[i]] = 0

ans = []
for i in range(m):
    if brr[i] not in dic:
        ans.append(0)
    else:
        ans.append(1)
        
print(*ans)
```