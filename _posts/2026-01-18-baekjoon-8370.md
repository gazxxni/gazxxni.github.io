---
layout: post
title: "[백준] 8370번 Plane (Python)"
date: 2026-01-18
categories: [Algorithm, Baekjoon]
tags: [python, algorithm, baekjoon, "8370", 수학, 사칙연산]
---

## 문제 링크
[https://www.acmicpc.net/problem/8370](https://www.acmicpc.net/problem/8370)

---

## 문제
Byteland Airlines recently extended their aircraft fleet with a new model of a plane. The new acquisition has n1 rows of seats in the business class and n2 rows in the economic class. In the business class each row contains k1 seats, while each row in the economic class has k2 seats.

Write a program which:


	reads information about available seats in the plane,
	calculates the sum of all seats available in that plane,
	writes the result.

---

## 입력
In the first and only line of the standard input there are four integers n1, k1, n2 and k2 (1 ≤ n1, k1, n2, k2 ≤ 1 000), separated by single spaces.

---

## 출력
The first and only line of the standard output should contain one integer - the total number of seats available in the plane.

---

## 성능 요약
메모리: 108384 KB, 시간: 88 ms

---

## 풀이

### 풀이 핵심 로직
이 문제의 핵심 아이디어는 비즈니스 클래스와 이코노미 클래스의 좌석 수를 각각 계산한 후, 두 값을 더하여 전체 좌석 수를 구하는 것입니다. 각 클래스의 좌석 수는 해당 클래스의 행 수와 각 행의 좌석 수를 곱하여 계산합니다.

### 동작 과정
1. 입력으로 주어지는 네 개의 정수 `n1`, `k1`, `n2`, `k2`를 읽어옵니다. 여기서 `n1`은 비즈니스 클래스의 행 수, `k1`은 비즈니스 클래스의 각 행에 있는 좌석 수, `n2`는 이코노미 클래스의 행 수, `k2`는 이코노미 클래스의 각 행에 있는 좌석 수입니다.
2. 비즈니스 클래스의 총 좌석 수는 `n1 * k1`로 계산합니다.
3. 이코노미 클래스의 총 좌석 수는 `n2 * k2`로 계산합니다.
4. 두 클래스의 좌석 수를 더하여 전체 좌석 수를 구합니다.
5. 최종 결과를 출력합니다.

예를 들어, 입력이 `2 3 4 2`라면:
- 비즈니스 클래스 좌석 수: `2 * 3 = 6`
- 이코노미 클래스 좌석 수: `4 * 2 = 8`
- 전체 좌석 수: `6 + 8 = 14`

### 시간 복잡도
이 알고리즘은 입력된 수치를 단순히 곱하고 더하는 연산만 수행하므로, 시간 복잡도는 O(1)입니다.

---

## 코드
```python
n1, k1, n2, k2 = map(int, input().split())

print(n1*k1 + n2*k2)
```